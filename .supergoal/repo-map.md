# Repository Map

```
d:\mysite
├── assets/                  # Public assets
│   ├── certificates/        # Coursera certificate images
│   └── video/               # Embeddable video reels
├── css/                     # CSS stylesheets
│   ├── index.css            # Base stylesheet & CSS variables
│   ├── components.css       # UI Components (glass cards, buttons, hexagons, dock)
│   ├── sections.css         # Section layouts (Hero, About, Projects, Skills, Contact)
│   └── animations.css       # Keyframes & scroll-reveal transitions
├── js/                      # Frontend JavaScript
│   ├── main.js              # Core UI orchestration (preloader, menu, nav, dock magnifier)
│   ├── cursor.js            # Custom cursor trail rendering
│   ├── particles.js         # 3D canvas background (Fibonacci sphere, grid, floating items)
│   ├── terminal.js          # Interactive terminal simulator (Web Audio, command execution)
│   └── typing.js            # Role-typing carousel animation
├── favicon.svg              # SVG website favicon
└── index.html               # Main website document
```
